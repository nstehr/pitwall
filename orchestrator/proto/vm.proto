syntax = "proto3";

package vm;

option go_package = "../vm";

message CreateVMRequest {
    string imageName = 1;
}
message VM {
    int64 id = 1;
    string imageName = 2;
    string status = 3;
}

service VMService {
    rpc CreateVM(CreateVMRequest) returns (stream VM) {} 
}